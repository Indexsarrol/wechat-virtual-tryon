{
  "name": "@virtual-tryon/admin",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --input-type=module -e \"import { startVitest } from 'vitest/node'; import vue from '@vitejs/plugin-vue'; const ctx = await startVitest('test', ['src/views/tasks/index.test.ts'], { environment: 'jsdom' }, { plugins: [vue()] }); const code = await ctx?.close(); process.exit(code ?? 0);\"",
    "vitest": "node --input-type=module -e \"import { startVitest } from 'vitest/node'; import vue from '@vitejs/plugin-vue'; const args = process.argv.slice(1); const filters = args.length > 0 ? args : undefined; const ctx = await startVitest('test', filters, { environment: 'jsdom' }, { plugins: [vue()] }); const code = await ctx?.close(); process.exit(code ?? 0);\"",
    "typecheck": "vue-tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "element-plus": "^2.11.4",
    "vue": "^3.5.13",
    "vue-router": "^4.6.3"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.4",
    "@vue/test-utils": "^2.4.6",
    "jsdom": "^26.1.0",
    "vite": "^6.3.5",
    "vitest": "^3.2.4",
    "vue-tsc": "^2.2.10"
  }
}
