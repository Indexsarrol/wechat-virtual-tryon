{
  "name": "@virtual-tryon/api",
  "private": true,
  "type": "module",
  "scripts": {
    "prisma": "prisma",
    "vitest": "vitest",
    "test": "vitest run src && vitest run --root ../.. tests/e2e/anonymous-flow.spec.ts",
    "typecheck": "tsc -p tsconfig.json",
    "prisma:generate": "prisma generate",
    "prisma:validate": "prisma validate"
  },
  "dependencies": {
    "@fastify/rate-limit": "^10.3.0",
    "@prisma/client": "^6.7.0",
    "fastify": "^5.2.1",
    "fastify-plugin": "^5.0.1"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@virtual-tryon/contracts": "workspace:*",
    "prisma": "^6.7.0",
    "tsx": "^4.21.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
